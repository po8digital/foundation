'use strict';

(function ($) {
  $('.gform_fields input, textarea').focus(function () {
    $(this).parent().prev().addClass('is-active');
  });
})(jQuery);

(function ($) {
  $('.blog .tease-post:nth-child(3),.blog .tease-post:nth-child(4), .blog .tease-post:nth-child(5)').addClass('tease-post--small');
  $('.tease-post--small').wrapAll("<div class='small-posts-wrapper'/>");
})(jQuery);

(function ($) {
  $(window).scroll(function () {
    if ($(this).scrollTop() >= 500) {
      $('.fixed-menu').addClass('isScrolled');
    } else {
      $('.fixed-menu').removeClass('isScrolled');
    }
  });
})(jQuery); //TODO: doesn't work don't know why

// based on https://gist.github.com/paulirish/12fb951a8b893a454b32
var $ = document.querySelector.bind(document);
var $$ = document.querySelectorAll.bind(document);

Node.prototype.on = window.on = function (name, fn) {
  this.addEventListener(name, fn);
};

NodeList.prototype.__proto__ = Array.prototype; // eslint-disable-line

NodeList.prototype.on = NodeList.prototype.addEventListener = function (name, fn) {
  this.forEach(function (elem) {
    elem.on(name, fn);
  });
};

var trigger = $('.header-menu__trigger');
var slideMenu = $('.slide-menu');
var close = $('.slide-menu__close'); // const itemWithChildrens = $('.menu-item-has-children');
// const openChildrensTrigger;
// console.log(dataset('after'));

var openMenu = function openMenu() {
  return slideMenu.classList.add('isOpen');
};

var closeMenu = function closeMenu() {
  return slideMenu.classList.remove('isOpen');
};

trigger.on('click', openMenu);
close.on('click', closeMenu); // itemWithChildrens.on('click', () => this.classList.add('itemOpen'));
//# sourceMappingURL=custom.min.js.map
