'use strict';

(function ($) {
  $('.gform_fields input, textarea').focus(function () {
    $(this).parent().prev().addClass('is-active');
  });
})(jQuery);

(function ($) {
  $('.blog .tease-post:nth-child(3),.blog .tease-post:nth-child(4), .blog .tease-post:nth-child(5)').addClass('tease-post--small');
  $('.tease-post--small').wrapAll("<div class='small-posts-wrapper'/>");
})(jQuery);

// based on https://gist.github.com/paulirish/12fb951a8b893a454b32
var $ = document.querySelector.bind(document);
var $$ = document.querySelectorAll.bind(document);

Node.prototype.on = window.on = function (name, fn) {
  this.addEventListener(name, fn);
};

NodeList.prototype.__proto__ = Array.prototype; // eslint-disable-line

NodeList.prototype.on = NodeList.prototype.addEventListener = function (name, fn) {
  this.forEach(function (elem) {
    elem.on(name, fn);
  });
};

var trigger = $('.header-menu__trigger');
var slideMenu = $('.slide-menu');
var close = $('.slide-menu__close');
var itemsWithChildrens = $$('.slide-menu .menu-item-has-children'); // const openChildrensTrigger;
// console.log(dataset('after'));

itemsWithChildrens.forEach(function (item) {
  return item.on('click', function (e) {
    if (e.offsetX > 300) {
      item.classList.toggle('active');
      console.log('clicked after');
    } else {
      console.log('clicked not after');
    }
  });
});

var openMenu = function openMenu() {
  return slideMenu.classList.add('isOpen');
};

var closeMenu = function closeMenu() {
  return slideMenu.classList.remove('isOpen');
};

trigger.on('click', openMenu);
close.on('click', closeMenu); // itemWithChildrens.on('click', () => this.classList.add('itemOpen'));

var cancel = $('.cancel-link');
var cancelLb = $('.cancel-lb');
var upgrade = $('.upgrade-link');
var upgradeLb = $('.upgrade-lb');
var closeCancel = $('.cancel-lb .close');
var closeUpgrade = $('.upgrade-lb .close');
var lbCancel = $('.cancel-lb');
var lbUpgrade = $('.upgrade-lb');

var closeLbCancel = function closeLbCancel() {
  return lbCancel.classList.remove('active');
};

var closeLbUpgrade = function closeLbUpgrade() {
  return lbUpgrade.classList.remove('active');
};

var buttonGreenCancel = $('.cancel-lb .button--green');
var buttonGreenUpgrade = $('.upgrade-lb .button--green');
cancel.on('click', function (e) {
  e.preventDefault();
  cancelLb.classList.add('active');
});
upgrade.on('click', function (e) {
  e.preventDefault();
  upgradeLb.classList.add('active');
});
closeCancel.on('click', closeLbCancel);
closeUpgrade.on('click', closeLbUpgrade);
buttonGreenCancel.on('click', closeLbCancel);
buttonGreenUpgrade.on('click', closeLbUpgrade);
//# sourceMappingURL=custom.min.js.map
